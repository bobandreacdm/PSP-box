name: Build PSP Homebrew

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest
    container: pspdev/pspdev:latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Build and Pack EBOOT.PBP
      run: |
        make clean
        make
        pack-eboot -s "PSPBox DJ" -o EBOOT.PBP PSPBox.elf

    - name: Upload EBOOT Artifact
      uses: actions/upload-artifact@v3
      with:
        name: PSPBox-EBOOT
        path: EBOOT.PBP
